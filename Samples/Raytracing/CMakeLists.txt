FILE(GLOB SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(RayTracing ${SOURCE_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(RayTracing vk-sample-common)
ADD_DEPENDENCIES(RayTracing vk-sample-common)

# Vulkan dependies headers.
TARGET_INCLUDE_DIRECTORIES(RayTracing PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

# # Generate the SPV object
INSTALL(TARGETS RayTracing DESTINATION bin)