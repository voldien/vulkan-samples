FILE(GLOB SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(StencilShadowVolume ${SOURCE_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(StencilShadowVolume vk-sample-common)
ADD_DEPENDENCIES(StencilShadowVolume vk-sample-common)

# Vulkan dependies headers.
TARGET_INCLUDE_DIRECTORIES(StencilShadowVolume PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS StencilShadowVolume DESTINATION bin)