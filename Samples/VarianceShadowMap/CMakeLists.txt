FILE(GLOB SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(VarianceShadowMap ${SOURCE_FILES} ${HEADER_FILES} )
TARGET_LINK_LIBRARIES(VarianceShadowMap vk-sample-common )
ADD_DEPENDENCIES(VarianceShadowMap vk-sample-common)

# Vulkan dependies headers.
TARGET_INCLUDE_DIRECTORIES(VarianceShadowMap PRIVATE ${CMAKE_CURRENT_SOURCE_DIR} )
