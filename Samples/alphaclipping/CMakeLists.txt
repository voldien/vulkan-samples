FILE(GLOB SOURCE_ALPHA_CLIPPING_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(AlphaClipping ${SOURCE_ALPHA_CLIPPING_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(AlphaClipping vk-sample-common)
ADD_DEPENDENCIES(AlphaClipping vk-sample-common)

# Vulkan dependies headers.
TARGET_INCLUDE_DIRECTORIES(AlphaClipping PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS AlphaClipping DESTINATION bin)