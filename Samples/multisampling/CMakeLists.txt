FILE(GLOB SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(MultiSampling ${SOURCE_FILES} ${HEADER_FILES} )
TARGET_LINK_LIBRARIES(MultiSampling vk-sample-common )
ADD_DEPENDENCIES(MultiSampling vk-sample-common)

# Vulkan dependies headers.
TARGET_INCLUDE_DIRECTORIES(MultiSampling PRIVATE ${CMAKE_CURRENT_SOURCE_DIR} )


INSTALL(TARGETS MultiSampling DESTINATION bin)