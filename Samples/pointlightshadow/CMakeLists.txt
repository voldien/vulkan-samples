FILE(GLOB SOURCE_POINT_LIGHT_SHADOW_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_POINT_LIGHT_SHADOW_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(PointLightShadow ${SOURCE_POINT_LIGHT_SHADOW_FILES} ${HEADER_POINT_LIGHT_SHADOW_FILES} )
TARGET_LINK_LIBRARIES(PointLightShadow vk-sample-common )
ADD_DEPENDENCIES(PointLightShadow vk-sample-common)

# Vulkan dependies headers.
TARGET_INCLUDE_DIRECTORIES(PointLightShadow PRIVATE ${CMAKE_CURRENT_SOURCE_DIR} )

INSTALL(TARGETS PointLightShadow DESTINATION bin)
