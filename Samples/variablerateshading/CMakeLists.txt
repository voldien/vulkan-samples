FILE(GLOB SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(VariableRateShading ${SOURCE_FILES} ${HEADER_FILES} )
TARGET_LINK_LIBRARIES(VariableRateShading vk-sample-common )
ADD_DEPENDENCIES(VariableRateShading vk-sample-common)

# Vulkan dependies headers.
TARGET_INCLUDE_DIRECTORIES(VariableRateShading PRIVATE ${CMAKE_CURRENT_SOURCE_DIR} )
