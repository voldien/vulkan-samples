FILE(GLOB SOURCE_VERTEX_INTERPOLATION_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_VERTEX_INTERPOLATION_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(VertexInterpolation ${SOURCE_VERTEX_INTERPOLATION_FILES} ${SOURCE_VERTEX_INTERPOLATION_FILES} )
TARGET_LINK_LIBRARIES(VertexInterpolation vk-sample-common )
ADD_DEPENDENCIES(VertexInterpolation vk-sample-common)

TARGET_INCLUDE_DIRECTORIES(VertexInterpolation PRIVATE ${CMAKE_CURRENT_SOURCE_DIR} )

INSTALL(TARGETS VertexInterpolation DESTINATION bin)